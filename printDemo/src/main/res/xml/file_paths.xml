<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!--  对应getFilesDir() -->
    <files-path name="filesDir" path="." />
    <!-- 对应getCacheDir() -->
    <cache-path name="cacheDir" path="." />
    <!-- 对应Environment.getExternalStorageDirectory() -->
    <external-path name="extStorageDir" path="." />
    <!-- 对应getExternalFilesDir() -->
    <external-files-path name="extFilesDir" path="." />
    <!-- 对应getExternalCacheDir() -->
    <external-cache-path name="extCacheDir" path="." />

    <root-path name="root_path"  path="." />
</paths>